read-only ${DOWNLOADS}
read-only ${MUSIC}
read-only ${PICTURES}
read-only ${VIDEOS}

whitelist ${HOME}/Pictures to sort
read-only ${HOME}/Pictures to sort
whitelist ${HOME}/Sync
read-only ${HOME}/Sync

# was hidden by globals.local
noblacklist /mnt
read-only /mnt
